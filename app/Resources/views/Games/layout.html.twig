{% extends 'base.html.twig' %}

{% block title %}
    Fat Boy Games
{% endblock %}

{% block body %}
    <div class="navbar navbar-default navbar-fixed-top " role="navigation" >
        <div class="container " >
            <div class="navbar-header " >
                <a class="navbar-brand " href="{{ path('homepage') }}" >
                    Fat Boy Games
                </a>
            </div>
            <div >
                <ul class="nav navbar-nav " >
                    <li >
                        <a href="{{ path('homepage') }}" >
                            Home
                        </a>
                    </li>
                </ul>
            </div>
            {{ include('User/navbarUserForm.html.twig') }}
        </div>
    </div>
    <div class="container">
        {{ include('default/alertMessage.html.twig') }}
    </div>
{% endblock %}